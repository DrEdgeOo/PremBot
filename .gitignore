node_modules/
.DS_Store
*.log
.vscode/
.idea/

# Local-only state (settings + transcript cache live in ~/.prembot, not the repo,
# but if anyone overrides paths during dev keep them out).
*.local.json
.prembot/
